const {chromium}=require('@playwright/test');const fs=require('fs');
(async()=>{const b=await chromium.launch({channel:'chrome'});try{const p=await b.newPage();let documents=0;p.on('request',r=>{if(r.isNavigationRequest()&&r.frame()===p.mainFrame())documents++});await p.goto('http://127.0.0.1:4313/search?lang=en',{waitUntil:'domcontentloaded'});await p.locator('#catalog-browse-form').waitFor();const before=documents;await p.waitForTimeout(5000);const result={importActive:true,observationMs:5000,additionalDocuments:documents-before};if(result.additionalDocuments)throw Error(JSON.stringify(result));fs.writeFileSync('docs/evidence/catalog-scale/artwork-import-no-reload.json',JSON.stringify(result,null,2));console.log(result);}finally{await b.close()}})();
