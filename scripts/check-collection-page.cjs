const{chromium,expect}=require('@playwright/test'),fs=require('fs');(async()=>{const b=await chromium.launch({channel:'chrome'});try{const results=[];for(const lang of['en','fr']){const p=await b.newPage({viewport:{width:390,height:844}});await p.goto('http://127.0.0.1:4313/collection?lang='+lang);await p.locator('.troc-collection-exercise').waitFor();await expect(p.locator('h1')).toHaveCount(1);expect(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);const s=p.locator('.troc-collection-exercise'),range=s.locator('input[type=range]');await range.focus();await p.keyboard.press('Home');await s.getByRole('button',{name:lang==='fr'?'Choisir un exemple adapté au budget':'Choose an example within budget',exact:true}).click();await expect(s.locator('.troc-collection-exercise-result')).toContainText(lang==='fr'?'Aucune carte':'No cards');await range.focus();await p.keyboard.press('End');await expect(s.locator('.troc-collection-exercise-result')).not.toContainText(lang==='fr'?'Aucune carte':'No cards');await s.getByRole('button',{name:lang==='fr'?'Choisir un exemple adapté au budget':'Choose an example within budget',exact:true}).click();await expect(s.locator('[role=checkbox][aria-checked=true]:not([disabled])')).toHaveCount(3);await s.getByRole('button',{name:lang==='fr'?'Réinitialiser cet exemple':'Reset this example',exact:true}).click();await expect(s.locator('[role=checkbox][aria-checked=true]:not([disabled])')).toHaveCount(0);await expect(range).toHaveValue('450');await s.screenshot({path:'docs/evidence/smart-education/collection-page-'+lang+'.png',style:'.troc-marketplace-header-frame{visibility:hidden!important}'});results.push({lang,oneHeading:true,noOverflow:true,zeroBudget:true,noStaleMessage:true,threeMissingCards:true,reset:true});await p.close()}fs.writeFileSync('docs/evidence/smart-education/collection-page-check.json',JSON.stringify(results,null,2));console.log(results)}finally{await b.close()}})().catch(e=>{console.error(e.message);process.exit(1)});