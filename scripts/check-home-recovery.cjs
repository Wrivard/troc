const {chromium,expect}=require('@playwright/test');
(async()=>{const b=await chromium.launch({channel:'chrome'});try{const p=await b.newPage();let fail=true;await p.route('**/api/catalog/page?**',async r=>fail?r.fulfill({status:503,contentType:'application/json',body:'{}'}):r.continue());await p.goto('http://127.0.0.1:4313/?lang=en');await expect(p.locator('.troc-home-hero')).toBeVisible();await expect(p.getByRole('alert')).toContainText('Card data could not load');await p.locator('.troc-home-hero').evaluate(e=>e.dataset.continuity='error-survives');fail=false;await p.getByRole('button',{name:'Try again',exact:true}).click();await expect(p.locator('[aria-busy]').first()).toHaveAttribute('aria-busy','false');await expect(p.locator('.troc-home-hero')).toHaveAttribute('data-continuity','error-survives');console.log('PASS homepage retained through503 and successful retry');}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
