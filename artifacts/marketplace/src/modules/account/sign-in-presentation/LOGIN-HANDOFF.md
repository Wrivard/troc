# Login presentation handoff

A-approved contract: `SignInLayout({locale,busy,children})` from modules/account/sign-in-presentation/SignInLayout. A alone conditionally replaces existing sign-in main with this component, passing existing busy/status nodes and unchanged sign-in form as children. The slot is a DIV, never a form. Layout owns main-content ID/h1/aria-busy; avoid nesting another main/h1. Keep existing MarketplaceHeader/Footer outside. Do not apply wrapper to signup/account/settings without separate scope. Header offset assumption88px; A may set the wrapper's min-height to actual header geometry in integration.

No auth state/effect/submit/API/validation/disabled/redirect/toggle/recovery implementation is added. Existing messages.signIn is reused, including Se connecter. Form labels/constraints/autocomplete and loading/status text remain caller-owned. Native invalid field gets a restrained error border; generic role=status remains neutral because the layout cannot infer error vs information. Existing absent password toggle/recovery flow is not fabricated. Real errors/hosted login/return routing remain A's behavior gate.

Full brief and supplied reference inspected. Exact bg-login.png copied, SHA256 A7CF57D0CD2D5AF1C0DA3B8F4BBC7D8C6ED1179C1BFB5E38581E9168CC25BC4D. Original1254square image preserved, cover crop70%65% desktop prioritizes card stack. No duplicated logo/extra image filters.40/60desktop,45/55small desktop, stack<=800, decorative image hidden<=500 for form priority as brief permits. Light preference preserved with neutral form inversion; image unchanged.

Author validation: marketplace typecheck/scoped lint PASS; actual isolated Vite build includes module and1,891.87kB exact image.20 cases1440/1024/768/390/320×ENFR×themes PASS: labels/native missing-email/type validation, autocomplete, one form, localized heading, Tab order/focus, mocked failure/status, disabled busy state, aria-busy, no overflow/pageerrors, axe WCAG2A/AA/2.1AA no violations. Test tests/sign-in-presentation-browser.mjs. The harness uses a copied sign-in-only field fixture and mocked failure handler, no auth requests/success claim. Does not certify integrated AccountApp behavior.

Evidence verification/sign-in-presentation/:20 normal captures and16 busy/error captures. Actual images inspected7:1440 ENdark normal (browser);1440 FRlight;1024 ENdark;768 FRdark;390 FRlight;390 ENdark error;1440 FRdark busy. Other captures not claimed visually inspected. Error/loading image inspections confirmed text is visible and inputs/CTA remain usable; actual200%zoom/text resize not executed.

Preview4341/?section=login&lang=en&theme=dark; state=busy or state=error. UI has no real login service. UX2 independent visual/state review, A actual form integration and B functional auth tests remain required. Homepage lots separately accepted/integrated Design1ad73c3e; no existing auth/global source edited here.

## P2L01 correction

UX2 observed loading status insertion moving form/submit21–42px. Scoped CSS now reserves48px before form in all states and positions ONLY the existing first loading status in that reserved area while aria-busy=true. No status node/handler/content removed or duplicated. Errors retain natural layout expansion.20 targeted normal→busy→normal cases1440/1024/768/390/320×ENFR×themes PASS: all input/submit x/y/width/height within1px, visible loading, disabled submit and no overflow. tests/sign-in-presentation-loading-browser.mjs; evidence loading-stability.json. Corrected desktopENdark and320FRlight busy images actually inspected. UX2 retest pending; real auth integration remains A-owned.
