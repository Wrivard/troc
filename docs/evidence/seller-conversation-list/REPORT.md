# Seller conversation list - backend local evidence
2026-09-24. New GET /seller/platform/:seller/conversations; existing UI/operations unchanged.

Default20/max50 rows; filter conversations/all and literal buyer/order/latest-body substring q(max100). Same existing active-store access plus orders:fulfill OR messages:reply; canReply separate. Latest message body/date/id come from one lateral row. Sort latest activity then orderID; cursor preserves microseconds and binds seller/filter/query/limit. Old orders with new activity are not excluded by order-age200cap. No cutoff pretending to be an MVCC snapshot: replies can reorder results; future UI must dedupe/refresh while preserving drafts.

Response is lean conversation metadata, without quote lines, catalogue artwork or full history. Per-user unread subqueries only run for bounded page+1 rows and retain actor exclusion/read receipts. List does not mark messages read. No message writes, stock/money changes or new indexes. Underlying latest-message scan/sort is not proven large-store fast; measured SQL strategy remains follow-up before capacity claims.

Test tests/seller-conversation-list.test.ts:320isolated orders,318eligible conversations; full17-row cursor traversal and uniqueness beyond200; oldest order receives newest reply and ranks first; tied microsecond activity; all-orders includes a no-message order; pending order excluded; literal wildcard/SQL-looking search; changed filters/query/limit cursor rejection; malformed inputs; per-user unread/read receipt separation; inventory/foreign/inactive denial; fulfillment read-only canReply; new reply refresh reorders; zero implicit read writes. In-process HTTP200bounded page/cursor and400invalid/duplicate inputs pass. APItypes/scopedlint/build pass.

No browser/UI integration, preview restart or production activation. NEXT: authorized independent selected-order context lookup, so deep links and selected conversations work outside visible pages; then wire existing Inbox with cursor list and independent detail, preserve drafts/retry keys/mobile/read-history. Legacy200disclosure remains until UI migrated. Then real SQL plan checks and Analytics contract. Hosted concurrency/remote gates unchanged.
