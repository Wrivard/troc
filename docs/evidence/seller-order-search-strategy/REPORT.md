# Bounded seller order search strategy
Date2026-09-24. Locally implemented list read; persistent preview/production not migrated or restarted.

order-list.ts now executes one statement with a MATERIALIZED probe of at most257matching IDs. At<=256 it uses the complete candidate ID array; at257 it selects the ordered broad scan with a correlated indexed projection lookup (OFFSET0 prevents the rejected semi-join plan). UNION branches have mutually exclusive one-time counts. Each has identical original seller/status/period/asOf/cursor filters, ordering and limit+1; outer ordering retains numeric total/date/ID semantics. The cap chooses an execution strategy only, never caps logical results. Values remain bound; locale chooses only validated en/fr column. Existing service access, cursor validation, fresh source joins and page-only artwork retained.

scripts/measure-seller-order-search-strategy.ts:36 independent legacy-expression full-row first-page parity comparisons across EN/FR,6query shapes and3sorts.60 integrated service checks include36first pages,18deep cursors after1000matches and6complete traversals across256/257thresholds for all sorts. Threshold traversal checks exact total and no duplicate IDs; first/deep page checks compare actual IDs/order.20k isolated fixtures, existing migrations, backend-role reads, database closed afterward. No catalogue/inventory writes. plans.json retains36measured candidate plans. Broad English around4ms, rare around1ms vs rejected broad~135ms plan; embedded samples only, not hosted SLA. Probe work on broad queries, short strings and millions of orders remains a measured scaling limitation.

seller-order-list.test.ts and seller-order-search.test.ts pass: permission/filter/cursor/HTTP/summary regression plus projection maintenance/privacy. APItypes, scopedlint and build pass. No UI changes; unchanged browser tests not rerun. Summary remains its previously verified once-per-row expression, so no new summary speed claim.

Requires migrations0024/0025 before runtime activation; neither applied to persistent preview/production. Multi-connection maintenance contention and deployment lock review still open. No native PostgreSQL tools available on PATH in last check, no retry loop. Away heartbeat reactivated5minutes at user request; no guaranteed uninterrupted execution.

NEXT: optimize summary search against projection without repeating broad-query regression; global priorities still require full-scope count. Preserve exact sums/refund union/sample rules and independent summary error UI. Then Messages/Analytics bounded contracts. Consider replacing brittle parameter-position knowledge with an explicit search binding contract only if additional query consumers require it.
