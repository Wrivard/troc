# Real draft conflict and schedule foundation

## Real promotion conflict lifecycle and schedule runtime foundation — September25

Real local synthetic seller sessions verified EN1440/FR390 UI409 after another session edited A and added B; read recovery makes no write, explicit resave persists local A and preserves B plus all preexisting drafts. Cleanup removes only testIDs via latest-version writes; original drafts preserved, immutable audit/version history retained. Evidence docs/evidence/promotions-conflict-real/check.json. Not native concurrent load/hosted certification.

Next dependency implemented locally: optional startsAt/endsAt canonical UTC instant fields on Promotion; start inclusive/end exclusive, both required if scheduled, malformed/partial/reversed schedule grants no discount. Legacy unscheduled rules unchanged. quoteCart captures one server time; optimizeCart shares one instant across original/candidate/naive quotes. Internal optional clock argument supports deterministic tests; no HTTP client clock field. No draft conversion/activation or stored live-rule writes.

9commerce tests pass including exact boundary, invalid schedule and original/optimized/naive discount parity plus existing checkout reservation/refund/credit and Smart Cart scenarios. Library declaration rebuild resolved stale type output; APItypes/scoped lint/build pass. API10136still previous runtime; schedule code not activated there. No schema change. Contract updated to distinguish delivered runtime foundation from remaining publishing work.

NEXT coherent batch: define strict versioned promotion publication command and review UI with explicit timezone/UTC date conversion disclosure, capacity20, role/coupon/date checks and transactional audit/idempotency; preserve existing live settings and quote locks. Draft date-only values must never be silently interpreted or automatically activated. Verify publication in disposable DB before deliberate local runtime activation.146IDs retained; E4.5 partial and remote/production/source/stock/money holds unchanged.
