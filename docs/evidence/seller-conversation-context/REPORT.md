# Selected conversation context - local evidence
2026-09-24. GET /seller/platform/:seller/conversations/:id implemented, UI integration pending.

Refactored existing operations reader into private readSellerOperations with optional exact order ID. Legacy sellerOperations wrapper/payload/200cap unchanged. Context wrapper validates UUID, reuses active-store and Orders-or-Messages authorization, constrains seller and exact order in SQL, excludes pending marketplace orders, returns404for missing/wrong-store/pending. Full quote-to-display mapping is reused, not duplicated. Only selected order's variant IDs reach existing canonical thumbnail resolver. No history payload or implicit read marker; unread/count/latest metadata matches existing semantics.

Focused test extends320-order fixture: legacy response remains200/truncated and excludes the deliberately oldest selected order; exact context still returns it with correct total,line,latest message. Exact thumbnail batch assertion, authorized-other-store404, cross-store cursor rejection, invalidUUID400, absent/pending404, inventory denial, in-process HTTP200/404 all pass. Existing list/unread/access tests pass. Analytics model regression2tests passes after shared operations-reader refactor. APItypes/scopedlint/build pass.

No new database migration, no preview restart, no browser UI switch, no real messages. Existing selected-message history/read endpoints remain unchanged. NEXT: wire existing SellerMessages to paged conversation list plus independent selected context; preserve draft/attempt state and desktop/mobile layout, independent error/loading, cursor reset, off-page deep link, reorder refresh, EN/FR. Remove200cap disclosure only for migrated Messages, leave Analytics. Query plans/hosted contention remain open.
