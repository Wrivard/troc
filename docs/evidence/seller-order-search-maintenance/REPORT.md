# Transactional seller order search - local checkpoint
2026-09-24. Implemented migration0025 and focused disposable tests; indexed application read intentionally not activated.

Migration: private seller_order_search with EN/FR text matching the existing exact concatenation. Transactional backfill under parent/child table locks; child insert/quote/parent/seller changes refresh projection, recipient edits refresh all linked sellers, deletion cascades. Parent rows locked in UUID order before child projection changes. Fixed search_path SECURITY DEFINER maintenance functions revoked from PUBLIC; backend SELECT only with RLS, no direct write or refresh grant. No stock/money fields changed. Production backfill lock duration and multi-connection deadlocks/freshness remain unverified.

Tests/seller-order-search.test.ts passes on disposable PGlite:40-row backfill; recipient edits; multilingual/missing-language/multiline quotes; rollback; seller reassignment; reparent; new child; shared-parent recipient update; blank recipient fallback; deletion; backend SELECT and denied direct DELETE/maintenance invocation; untrusted read denial; RLS enabled. Seller-order-list regression also passes251eligible orders, pagination/roles/summary exactness. Typecheck and scoped lint pass. No persistent preview migrations/restart or production changes.

Read integration experiment: EXISTS against indexed projection plus separate filtered/global summary aggregates tested, then reverted after measured regression. Plans-indexed-read.json retains30plans. With fresh statistics, broad English first page~135ms compared to prior~0.15ms. Planner estimated2matches for20k and chose projection-first nested loops+sort. Rare literal50%_ chose order-first20klookups~56ms; absent~0.8ms. Global-priority summary still~44ms even for rare query. Therefore index existence alone is insufficient. The previous correct read and once-per-row summary remain active in source. Experimental read source is no longer application code. Candidate predicate was EXISTS(SELECT1 FROM seller_order_search search WHERE search.order_id=so.id AND search.seller_id=so.seller_id AND search.en LIKE $pattern), with validated en/fr column selection.

Probe instrumentation now analyzes the new table after fixture inserts; first run before ANALYZE was not accepted. Next plan runs write plans-next-run.json to preserve the rejected candidate evidence. Historical substring probe freezes its original source expression for future parity comparisons.

Native postgres/pg_ctl/docker are not available through current PATH discovery; no contention retry loops. This is an environment limitation, not proof no executable exists anywhere. Hosted concurrency remains blocked. PGlite serial tests do not certify multi-connection locks. No new dependency/service installed.

NEXT: single-statement bounded candidate probe (e.g.257IDs) selecting rare-ID seek vs broad ordered scan, without dropping matches or relying on an arbitrary candidate cap. Preserve same-statement visibility; do not make two nontransactional reads that can omit updates. Measure both broad and rare, all3sorts/deep cursors and full summary semantics before re-enabling reads. Then Messages/Analytics contracts. Existing remote/source/auth holds remain.
