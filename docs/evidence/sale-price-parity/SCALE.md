# Effective-price plan evidence

## Effective-price scale probe and seller dependency reconciliation - September25

Measured actual catalogue search/detail methods in a disposable PGlite database:10,000 synthetic SQL products/60,000offers, including10,005offers for one variant and mixed sale/regular prices. These fixtures never enter the application catalogue. Compared existing indexes with an effective-price expression index; asserted identical ordered IDs/prices/quantities. Three-run observations: global price search ~113ms versus110ms (no meaningful improvement); bounded price filter6.5->4.6ms; popular-variant full detail15.6->10.8ms ascending,16.1->10.7ms descending. Isolated offer-plan execution7.449->0.039ms ascending and6.456->0.088ms descending,337->5shared hit blocks. Ascending index scan and descending incremental sort replace full top-N sorting. Warm sequential local observations, not native/hosted/million-row or causal latency guarantees.

Evidence: Troc-UX-Design/scripts/measure-sale-price-plans.ts and docs/evidence/sale-price-parity/plans.json. Added0031_listing_effective_price_index.sql with same measured expression, active/nonzero partial predicate and stable ID tie-breaker; retains existing regular-price index.21catalogue regression tests apply migration and pass; benchmark lint passes.0031 NOT yet applied to persistent API57684; no runtime/catalogue/stock mutations this batch. Next local step: controlled activation/readiness plus catalogue smoke; global price minima/projection scale remains separate, no speculative index claims.

Reconciled E4.3/UP01: photo adapter only reads URLs; upload/decode/quarantine/owned publication are absent implementations, not just an unavailable host. Existing manual flow already discloses photo drafts and raw-single-only scope. docs/SELLER-LISTING-DEPENDENCIES.md records photo/graded/offer gates. Promoted-percentage contract written; charging bounds/attribution/consent unresolved, existing fees untouched. Removed stale sale-editor-next text from E4.5 remaining ledger.146IDs retained. After0031activation continue the dependency-ready UP01 storage lifecycle contract/local validation foundation; do not expose unsupported upload or offer controls.

