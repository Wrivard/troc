# Seller Messages pagination integration - local evidence
2026-09-24. UI source and local preview connected.

SellerMessages now uses useConversationPage and independent useConversationContext.20-row Previous/Next cursor navigation, server search with180mscoalescing, reset on filter changes, explicit refresh to first page. Existing controls stay mounted during list loading/error. Selection/context remain independent of list page/filter/error; draft and retry-attempt maps remain mounted across those transitions and selecting other conversations. Context failures have separate retry/back, no misleading successful empty state. Latest-only effects ignore late responses. Existing paged message history/read actions/send authorization unchanged. Pre-sale tab mounts without legacy order reads. Existing channel/store discard confirmation behavior retained; no new promise of drafts surviving page reload/channel discard.

List/context skeletons, bounded scrollable list,44pxpager controls and sticky footer. Existing desktop columns and mobile list/detail/back preserved. Legacy200disclosure removed from Messages only; Analytics unchanged. Sample-order disclosure retained. No implied full-history count. Refresh handles activity reorder; cursors are not MVCC snapshots.

check-seller-conversations-paged.cjs: intercepted209-row EN1440/FR390,20-row list, off-page selected context503/retry, Next/Previous, selected draft survives pagination/search/list503retry/empty, slow response ignored after fast query, selected change, failed send preserves draft and exact idempotency key, zero browser legacy operations reads and no viewport overflow. All sends intercepted503; no real messages. EN/FR screenshots actually opened and inspected (scrolled conversation/error/pager views). New layout verification is not broad accessibility certification.

check-seller-conversations-live.cjs: real local2conversations, exact list count, direct selected-order navigation and composer, no legacy API request, no POSTs; order-list and order-summary searched HTTP200 after local migration. First live test falsely counted source asset operations-ui.tsx as an operations API request; inspected URL evidence, tightened matcher to actual API route, then pass. This was test instrumentation, not an app regression. Earlier Windows rg glob failed without modifications.

Frontend typecheck/scopedlint and final client+SSR builds pass (.local/conversations-paged-build.log; existing bundle size warnings remain). Backend list/context/maintenance regressions from prior checkpoints retained, unchanged backend not rerun. New files use-conversation-page.ts and browser checks; existing SellerMessages.tsx and scoped seller-operations.css updated.

Preview activation: verified owned PID49184 and port5313 before stop; launched same local-only entry point hidden as PID54900. Script rejects hosted DB/Auth configuration. Existing migration runner applied0024/0025 transactionally to persistent isolated test DB; no production migration. Catalogue/listing/planner revisions skipped unchanged. Logs .local/conversations-api.log and conversations-api-error.log. Local auth sessions reset on API restart. No duplicate process/database opener.

NEXT: measure actual conversation-list latest/unread query plans on disposable larger histories, then Analytics aggregate contract. Global exact order summaries still scan history. Hosted multi-connection contention and full-capacity certification remain blocked. User returned: away heartbeat PAUSED.
