# Seller order summary — local verification
Added separate authorized GET /seller/platform/:seller/order-summary. Reuses order-list filter SQL and parser; optional validated asOf permits matching list/summary cutoff and rejects conflicting cursor cutoffs.
Database aggregates include full eligible history, grouped into global priority counts, search/period tab counts, selected-status matched count/CAD total and matched demo presence. No thumbnails/message reads or200cap. BigInt accumulation rejects unsafe monetary/count serialization instead of rounding. No UI wiring or hosted scale claims yet.
Disposable PGlite test verifies251eligible rows, exact sum,167combined full/partial refunds, filtered vs global scope, literal wildcard search, EN/FR search, empty state, historical microsecond cutoff, demo classification, access denial, HTTP route, invalid dates/cursor and unsafe aggregate. Full cursor regression retained. Initial fixture attempted to update immutable demo provenance; corrected to set provenance at INSERT, with no constraint changes.
APItypes, scoped lint and build pass. No production DB/message/payment changes. Next Orders-only integration, independent read/error states and page-scoped CSV.
