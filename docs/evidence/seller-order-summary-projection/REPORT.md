# Seller order summary projection - local evidence
2026-09-24. order-summary.ts now reads existing private projection text instead of decoding each quote's line names. Only searched summaries join the projection; no-query plan remains unchanged. LEFT JOIN preserves full-scope priorities even when an index row is absent, while filtered matching requires projection text. Source orders still own seller/status/date/amount/demo; same asOf, permissions, refund union and safe BigInt serialization.

20k disposable fixture plans: searched English95.422ms, literal50%_94.756ms, absent98.542ms. Previous once-per-row JSON search145-149ms. No-query54-56ms; compared against earlier~53-56ms, no meaningful regression. Single embedded samples, not hosted SLA. Unlike previous selective filtered/global split (~197ms broad), this plan improves broad and rare searches consistently. Exact global counts remain O(history); no constant-time/million-store claim.

Checks: seller-order-list.test.ts and seller-order-search.test.ts pass. Existing summary checks cover251eligible orders, exact totals,167combined refunds, global vs selected filters, historical cutoff, EN/FR and literal wildcards, demo scope, overflow, allowed/denied roles and HTTP. Projection tests cover source updates/rollback/delete/private access. APItypes/scopedlint/build pass. Query-plan harness captures30plans including list/deep queries alongside changed summary. No UI change; unchanged browser matrices not rerun.

Requires pending0024/0025 migrations; persistent preview not migrated/restarted. No production changes. Hosted concurrency and schema activation remain separate. NEXT: Messages bounded inbox list and independent selected-order detail, preserving existing paged history/read receipts and drafts. Contract PERFORMANCE-AUDIT/SELLER-MESSAGES-ANALYTICS-SCALE-CONTRACT.md. Full-history aggregate scaling remains an explicit follow-up, not a reason to duplicate delivered UI.
