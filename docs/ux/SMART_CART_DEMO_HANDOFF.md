# Smart Cart illustrative walkthrough — UX47 / SC03

2026-09-22. Implements the exact A presentation grant: new brand/SmartCartDemo.tsx and CommerceApp empty-SmartCart JSX/import only. Filled-cart controllers, quote freshness, persistence, analytics, API contracts and purchase/apply behavior are unchanged. No custom styling or new design-system primitive: composes the approved Button and SmartCartComparison already documented at /style-guide editorial.

Empty Smart Cart now discloses an illustrative before-tax CAD scenario before controls, offers Explore an example and locale-preserving Browse cards, and expands a complete side-by-side comparison with three guided steps. Reset selects the original-basket explanation; Close collapses the example. Scenario:675+750=1425 vs722+400=1122 cents; savings303 = shipping350 less merchandise increase47. It illustrates grouping the same quantities with one seller, with no actual offers, condition substitutions or eligibility claim. Real seller/printing/language/condition/quantity/minimum/shipping review and explicit application remain explained.

Author validation PASS: marketplace typecheck, workspace lint, client and SSR builds. tests/smart-cart-demo-preview.mjs:12cells390/1440/320 × ENFR × light/dark;4mobile homepage->SmartCart keyboard entry journeys; Enter disclosure/step, reset, close, locale catalog link, exact displayed integer-cent totals and no horizontal overflow. Existing anonymous-cart GET and event requests intercepted; demo API provides quote reads. After initial network idle, all example actions make ZERO network requests and cart localStorage remains byte-identical (including original '[ ]' whitespace). Axe scoped scans390FR light/dark have zero violations. Initial harness failures were renamed-button locator and axe explicit-context requirements, corrected before final passing run.

Actual images inspected: verification/smart-demo-390-fr-dark.png, smart-demo-1440-en-light.png, smart-demo-320-fr-light.png. Other matrix screenshots captured and behavior-tested, not claimed visually inspected. Browser automated keyboard checks are not screen-reader testing. tests/cart-quote-freshness-preview.mjs existing populated route/Smart flow regression PASS with intercepted mutations and out-of-order responses.

Owned preview restored after compaction: UI4313 session74339, demo/noDB API5313 session56410; HTTP200 confirmed. No deployment/push. Independent UX47 and B wiring review required before A integration. Hero81133f8 unchanged in this candidate; newly reported HE01 mobile outline correction will be separate.
