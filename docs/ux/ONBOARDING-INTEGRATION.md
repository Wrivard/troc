# Unified current-site onboarding checkpoint

Use http://127.0.0.1:4313/sign-up (current Design checkout). 5187 is the separate milestone checkout and retains its older homepage. Current HomeSections SHA verified identical to the pre-integration backup manifest. Existing targets preserved at ../integration-backups/current-site-onboarding-20260923 before narrowly merging route wrappers and reviewed backend diffs; no homepage or index replacement.

Authentication is first: existing verified session -> questions; Google OAuth returns only to allowlisted /early-access; email signup confirmation can return to the same fixed onboarding destination. Password sign-in honors only this explicit return target. Questions bind the contact email to the signed-in session in UI. Thank-you follows receipt persistence and contains no account-creation form. Anonymous interest storage remains unverified acquisition data; no claim of server-verified survey linkage or seller activation.

Local verification: four authenticated buyer/seller/both flows 1440/768/390/320 EN/FR pass, no horizontal overflow, scoped axe0; anonymous Google/email entry, retry and confirmation handoff pass at1440/390. Twelve backend tests pass including real SDK simulated-provider PKCE binding and prelaunch transactions/validation. Frontend and API typecheck, focused lint, client/SSR builds pass. Rendered current home and signup inspected. Vite optimized dependencies refreshed after new imports.

Local API restarted on5313 with demo catalogue and APP_ORIGIN4313. Catalogue200; providers returns google:false. Live auth/database and provider configuration remain blocked; no real accounts, emails, provider changes or deployment performed. Earlier broad replacement attempts were rejected by automatic review; integration completed safely through exclusive new files, preserved originals and contextual targeted diffs.

## Shared signup/sign-in spacing
The current4313 site now uses SignInLayout for sign-in as well as signup, preserving the existing auth controller and form nodes. Both share the supplied right-side image and responsive layout. Intro clearance increased to65px desktop1440,45px tablet834,41px mobile390 (measured navbar bottom to heading). Reduced the excessive header-to-fields gap; removed the stale mobile22px override. Desktop/tablet EN-dark/FR-light and mobile checks show no horizontal overflow; sign-in failed-request state retains email. Typecheck and focused lint pass; actual desktop sign-in and mobile signup screenshots inspected. Live auth remains configuration-blocked.

## Sign-in reference styling
Sign-in left pane now uses a centered384px column, concise28px heading/support, outlined neutral Google action, separate or divider,40px fields,44px red submit, password visibility toggle and centered registration prompt. Desktop artwork remains the same; rules are scoped to sign-in. EN dark1440 and FR light390 rendered captures inspected, no overflow; password reveal/hide and failed-auth input retention checked. Typecheck/lint passed. No unsupported providers or fake recovery links were added.

### Canadian signup and sign-in polish — September 23
- Current preview4313: removed sign-in Canada checkbox, restored white Google button, separated muted prompt and underlined signup link.
- Signup requires street/unit, city, province/territory and matching Canadian postal code before either Google or email registration. Address is transient, not saved in browser storage, cookies, profile or URL. This is plausibility checking, NOT proof of residency or occupancy. Stronger verification and provider-level direct registration policy remain production work.
- Google distinguishes signin/signup. Signin requires an existing TROC user before committing session cookies. Signup validates address before OAuth; HMAC binds flow and intent against cookie tampering. Production requires securely generated AUTH_FLOW_SECRET (32+ characters, shared across instances, never VITE-exposed). Development uses process-local key; restart fails closed for in-flight OAuth.
- Four focused address/OAuth tests PASS, types PASS, targeted lint PASS, client/SSR build PASS. Mocked1440EN/390FR signup and signin checks PASS; desktop signin/mobile signup renders inspected. Running local API reloaded. No real accounts or Google sessions created; hosted auth/provider/database remain unconfigured.
