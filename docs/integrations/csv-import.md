# CSV import contract

Status: **In development**.

See [seller import instructions](../sellers/bulk-import.md) for fields, limits and error handling, and [inventory architecture](../architecture/inventory.md) for transactions, ownership and identifiers. Reusable seller-scoped column mappings are the extension point for future exported formats. Do not claim compatibility with an external product without testing its real exported schema.
